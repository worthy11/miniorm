from setuptools import setup, find_packages

setup(
    name="miniorm",
    version="0.1.0",
    description="A lightweight Python ORM",
    author="Your Name",
    packages=find_packages(),
    python_requires=">=3.7",
    install_requires=[],
)
